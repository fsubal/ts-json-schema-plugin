{
  "name": "ts-json-schema-plugin",
  "version": "1.0.0",
  "description": "Language Server Plugin that checks a type declaration matches a JSON Schema",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@jsdoc/parse": "^0.3.11",
    "typescript": "^5.6.2"
  }
}
